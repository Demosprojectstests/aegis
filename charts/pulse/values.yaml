gateway:
  replicaCount: 2
  image: pulse-gateway:dev
  port: 8080

orders:
  replicaCount: 2
  image: pulse-orders:dev
  port: 8080

worker:
  replicaCount: 1
  image: pulse-worker:dev
  port: 8080

redis:
  image: redis:7-alpine
  port: 6379

ingress:
  host: pulse.127.0.0.1.nip.io
